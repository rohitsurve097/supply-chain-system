apiVersion: v1
kind: Service
metadata:
  name: shipment-service
  namespace: supply-chain
spec:
  type: ClusterIP
  selector:
    app: shipment-service
  ports:
    - name: http
      port: 80
      targetPort: 8003
